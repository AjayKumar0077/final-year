import { SignupForm } from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up - FoodBridge',
  description: 'Create your FoodBridge account',
};

export default function SignupPage() {
  return <SignupForm />;
}
