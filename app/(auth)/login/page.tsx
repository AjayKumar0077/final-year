import LoginFormClientOnly from './LoginFormClientOnly';

export const metadata = {
  title: 'Login - FoodBridge',
  description: 'Log in to your FoodBridge account',
};

export default function LoginPage() {
  return <LoginFormClientOnly />;
}
