import { NGODashboard } from '@/components/dashboards/NGODashboard';

export const metadata = {
  title: 'NGO Command Center - FoodBridge',
  description: 'Manage food distribution operations',
};

export default function NGOPage() {
  return <NGODashboard />;
}
